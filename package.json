{
  "name": "movietrakk-client-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand --coverage",
    "test:client": "jest --runInBand --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
